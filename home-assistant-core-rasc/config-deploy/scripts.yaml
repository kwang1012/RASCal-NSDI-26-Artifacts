light_up_script:
  alias: Light up script
  sequence:
  - service: light.turn_on
    data:
      brightness_pct: 50
      transition: 8
    target:
      entity_id: light.virtual_light
  mode: single
  icon: mdi:alarm-light
